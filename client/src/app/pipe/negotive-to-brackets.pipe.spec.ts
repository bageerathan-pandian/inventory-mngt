import { NegotiveToBracketsPipe } from './negotive-to-brackets.pipe';

describe('NegotiveToBracketsPipe', () => {
  it('create an instance', () => {
    const pipe = new NegotiveToBracketsPipe();
    expect(pipe).toBeTruthy();
  });
});
